import styled from 'styled-components'

const Badge = styled.div`
  padding: 4px 12px;
  color: white;
  display: inline-block;
  border-radius: var(--border-radius-base);
  background-color: var(--pamary-color);
`

export default Badge
